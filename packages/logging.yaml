logger:
  default: info

# Possible log severities are:
#
# critical
# fatal
# error
# warning
# warn
# info
# debug
# notset

influxdb:
 host: 192.168.33.2
 username: 'homeassistant'
 password: !secret influxdb_password
 exclude:
   domains:
      - automation
      - lights
      - switch
      - group
      - scene
      - script
      - zwave

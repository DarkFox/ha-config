logger:
  default: error

# Possible log severities are:
#
# critical
# fatal
# error
# warning
# warn
# info
# debug
# notset

influxdb:
  host: addon_53caca22_influxdb
  username: 'homeassistant'
  password: !secret influxdb_password
  exclude:
    domains:
      - group
      - scene
      - zwave
    entities:
      - sensor.s_train_b_next
      - sensor.time
      - sensor.date

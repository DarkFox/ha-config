template:
  - binary_sensor:
      - name: Quest In Use Delayed
        state: "{{ is_state('binary_sensor.quest_in_use', 'on') }}"
        icon: mdi:virtual-reality
        delay_off:
          seconds: 30

script:
  light_profile_living_room_bright:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          color_temp: 370
          entity_id:
            - light.sofalampe
            - light.spisebord
            - light.projektor
      # Dimmable Lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          entity_id:
            - light.vitrineskab
            - light.cabinet_shelves
            - light.cabinet_spots
      # Primary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          hs_color: [0, 0]
          entity_id:
            - light.globe
            - light.sofa_corner
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          hs_color: [29, 15]
          entity_id:
            - light.bar
            - light.display_cabinet_top
            - light.display_cabinet_bottom
            - light.tv_shelf
            - light.tv_backlight
      # IKEA lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          entity_id:
            - light.stue_spots
            - light.sofa_laeselampe
      - delay:
          milliseconds: "{{ ((((transition_time or 0.5) | float) + 2.0) * 1000) | int }}"
      - service: light.turn_on
        data_template:
          transition: 0
          brightness: 255
          color_temp: 370
          entity_id:
            - light.stue_spots
            - light.sofa_laeselampe

  light_profile_living_room_default:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 128
          color_temp: 430
          entity_id:
            - light.sofalampe
            - light.spisebord
      # Dimmable Lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 127
          entity_id:
            - light.vitrineskab
            - light.cabinet_shelves
            - light.cabinet_spots
      # Primary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 192
          hs_color: [ "{{ states.sensor.theme_primary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color.state.split(',')[1]|int }}"]
          entity_id:
            - light.tv_backlight
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          hs_color: [ "{{ states.sensor.theme_primary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color.state.split(',')[1]|int }}"]
          entity_id:
            - light.display_cabinet_top
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 192
          hs_color: [ "{{ states.sensor.theme_primary_color_hue.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color_hue.state.split(',')[1]|int }}"]
          entity_id:
            - light.globe
            - light.projektor
      # Secondary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          hs_color: [ "{{ states.sensor.theme_secondary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_secondary_color.state.split(',')[1]|int }}" ]
          entity_id:
            - light.bar
            - light.sofa_corner
            - light.display_cabinet_bottom
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 192
          hs_color: [ "{{ states.sensor.theme_secondary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_secondary_color.state.split(',')[1]|int }}" ]
          entity_id:
            - light.tv_shelf
      # IKEA lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 72
          entity_id:
            - light.stue_spots
            - light.sofa_laeselampe
      - delay:
          milliseconds: "{{ ((((transition_time or 0.5) | float) + 2.0) * 1000) | int }}"
      - service: light.turn_on
        data_template:
          transition: 0
          brightness: 72
          color_temp: 430
          entity_id:
            - light.stue_spots
            - light.sofa_laeselampe

  light_profile_living_room_dimmed:
    sequence:
      # Off Lights
      - service: light.turn_off
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          entity_id:
            - light.stue_spots
            - light.sofa_laeselampe
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 76
          color_temp: 500
          entity_id:
            - light.sofalampe
            - light.spisebord
      # Dimmable Lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 32
          entity_id:
            - light.cabinet_shelves
            - light.cabinet_spots
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 16
          entity_id:
            - light.display_cabinet_front_1
            - light.display_cabinet_front_4
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 42
          entity_id:
            - light.display_cabinet_front_2
            - light.display_cabinet_front_3
      # Primary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 127
          hs_color: [ "{{ states.sensor.theme_primary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color.state.split(',')[1]|int }}"]
          entity_id:
            - light.tv_backlight
            - light.display_cabinet_top
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 127
          hs_color: [ "{{ states.sensor.theme_primary_color_hue.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color_hue.state.split(',')[1]|int }}"]
          entity_id:
            - light.globe
            - light.projektor
      # Secondary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 127
          hs_color: [ "{{ states.sensor.theme_secondary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_secondary_color.state.split(',')[1]|int }}" ]
          entity_id:
            - light.bar
            - light.tv_shelf
            - light.sofa_corner
            - light.display_cabinet_bottom

  light_profile_living_room_ambient:
    sequence:
      # Off Lights
      - service: light.turn_off
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          entity_id:
            - light.stue_spots
            - light.cabinet_spots
            - light.display_cabinet_front_1
            - light.display_cabinet_front_4
            - light.cabinet_shelves
            - light.sofa_laeselampe
      # Dimmable lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 16
          entity_id:
            - light.display_cabinet_front_2
            - light.display_cabinet_front_3
      # Primary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 64
          hs_color: [ "{{ states.sensor.theme_primary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color.state.split(',')[1]|int }}"]
          entity_id:
            - light.display_cabinet_top
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 48
          hs_color: [ "{{ states.sensor.theme_primary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color.state.split(',')[1]|int }}"]
          entity_id:
            - light.tv_backlight
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 72
          hs_color: [ "{{ states.sensor.theme_primary_color_hue.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color_hue.state.split(',')[1]|int }}"]
          entity_id:
            - light.projektor
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 32
          hs_color: [ "{{ states.sensor.theme_primary_color_hue.state.split(',')[0]|int }}", "{{ states.sensor.theme_primary_color_hue.state.split(',')[1]|int }}"]
          entity_id:
            - light.sofalampe
            - light.globe
      # Secondary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 32
          hs_color: [ "{{ states.sensor.theme_secondary_color_hue.state.split(',')[0]|int }}", "{{ states.sensor.theme_secondary_color_hue.state.split(',')[1]|int }}"]
          entity_id:
            - light.spisebord
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 32
          hs_color: [ "{{ states.sensor.theme_secondary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_secondary_color.state.split(',')[1]|int }}" ]
          entity_id:
            - light.tv_shelf
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 64
          hs_color: [ "{{ states.sensor.theme_secondary_color.state.split(',')[0]|int }}", "{{ states.sensor.theme_secondary_color.state.split(',')[1]|int }}" ]
          entity_id:
            - light.bar
            - light.sofa_corner
            - light.display_cabinet_bottom

  light_profile_living_room_night:
    sequence:
      # Off Lights
      - service: light.turn_off
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          entity_id:
            - light.stue_spots
            - light.sofalampe
            - light.cabinet_spots
            - light.cabinet_shelves
            - light.vitrineskab
            - light.spisebord
            - light.globe
            - light.sofa_laeselampe
      # Primary RGB lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 48
          rgb_color: [255,0,0]
          entity_id:
            - light.tv_shelf
            - light.bar
            - light.sofa_corner
            - light.display_cabinet_top
            - light.display_cabinet_bottom
            - light.tv_backlight
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 2
          rgb_color: [255,0,0]
          entity_id:
            - light.projektor

  light_profile_living_room_off:
    sequence:
      # Off Lights
      - service: light.turn_off
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          entity_id: group.living_room_lights

  light_profile_living_room_party:
    sequence:
      # Off Lights
      - service: light.turn_off
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          entity_id:
            - light.stue_spots
            - light.cabinet_spots
      # Dimmable Lights
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 2
          entity_id:
            - light.vitrineskab
            - light.cabinet_shelves
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          effect: "rainbow"
          entity_id:
            - light.tv_shelf
            - light.sofa_corner
            - light.projektor
            - light.bar
            - light.tv_backlight
            - light.display_cabinet_top
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          effect: "colorloop"
          entity_id:
            - light.sofalampe
            - light.globe
            - light.spisebord

input_boolean:
  living_room_activity_override:
    icon: mdi:checkbox-multiple-marked
    name: Livingroom Activity Override
  living_room_separate_light_profile:
    name: Living Room Separate Light Profile
    icon: mdi:heart-broken

input_select:
  living_room_room_state:
    name: Livingroom State
    options:
      - active
      - inactive
      - 'off'

  living_room_light_profile:
    name: living room Light Profile
    options:
      - Bright
      - Default
      - Dimmed
      - Ambient
      - Night
      - 'Off'

  living_room_inactive_light_profile:
    options:
      - Default
      - Dimmed
      - Ambient
      - Night
      - 'Off'

light:
  - platform: template
    lights:
      living_room_activity_override:
        friendly_name: Light Living Room Activity Override
        icon_template: mdi:checkbox-multiple-marked
        value_template: "{{ is_state('input_boolean.living_room_activity_override', 'on') }}"
        turn_on:
          service: input_boolean.turn_on
          data:
            entity_id: input_boolean.living_room_activity_override
        turn_off:
          service: input_boolean.turn_off
          data:
            entity_id: input_boolean.living_room_activity_override

      living_room_separate_light_profile:
        friendly_name: Light Living Room Separate Light Profile
        icon_template: mdi:heart-broken
        value_template: "{{ is_state('input_boolean.living_room_separate_light_profile', 'on') }}"
        turn_on:
          service: input_boolean.turn_on
          data:
            entity_id: input_boolean.living_room_separate_light_profile
        turn_off:
          service: input_boolean.turn_off
          data:
            entity_id: input_boolean.living_room_separate_light_profile

      living_room_light_profile_bright:
        friendly_name: Lyst
        icon_template: "mdi:brightness-7"
        value_template: "{{ is_state('input_select.living_room_light_profile', 'Bright') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.living_room_light_profile
            option: Bright
        turn_off:
          event: living_room_light_profile_off_null

      living_room_light_profile_default:
        friendly_name: Normal
        icon_template: "mdi:brightness-5"
        value_template: "{{ is_state('input_select.living_room_light_profile', 'Default') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.living_room_light_profile
            option: Default
        turn_off:
          event: living_room_light_profile_off_null

      living_room_light_profile_dimmed:
        friendly_name: Dæmpet
        icon_template: "mdi:brightness-6"
        value_template: "{{ is_state('input_select.living_room_light_profile', 'Dimmed') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.living_room_light_profile
            option: Dimmed
        turn_off:
          event: living_room_light_profile_off_null

      living_room_light_profile_ambient:
        friendly_name: Baggrund
        icon_template: "mdi:brightness-4"
        value_template: "{{ is_state('input_select.living_room_light_profile', 'Ambient') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.living_room_light_profile
            option: Ambient
        turn_off:
          event: living_room_light_profile_off_null

      living_room_light_profile_night:
        friendly_name: Nat
        icon_template: "mdi:brightness-3"
        value_template: "{{ is_state('input_select.living_room_light_profile', 'Night') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.living_room_light_profile
            option: Night
        turn_off:
          event: living_room_light_profile_off_null

      living_room_light_profile_off:
        friendly_name: 'Slukket'
        icon_template: "mdi:power"
        value_template: "{{ is_state('input_select.living_room_light_profile', 'Off') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.living_room_light_profile
            option: 'Off'
        turn_off:
          event: living_room_light_profile_off_null

binary_sensor:
  - platform: template
    sensors:
      living_room_activity:
        friendly_name: 'Livingroom Activity'
        entity_id:
          - binary_sensor.livingroom_sensor
          - binary_sensor.spisebord_bevaegelse
          - binary_sensor.sofagruppe_bevaegelse
          - input_boolean.living_room_activity_override
          - input_select.living_room_state
          - media_player.stue_chromecast
          - input_select.latest_motion
        value_template: >
          {%- if is_state('input_boolean.living_room_activity_override', 'on') -%}
            {{ 'true' if is_state('input_select.living_room_room_state', 'active') else 'false' }}
          {%- elif is_state('binary_sensor.livingroom_sensor', 'on')
          or is_state('binary_sensor.spisebord_bevaegelse', 'on')
          or is_state('binary_sensor.sofagruppe_bevaegelse', 'on')
          or is_state('media_player.stue_chromecast', 'playing')
          or is_state('media_player.stue_chromecast', 'paused')
          or is_state('input_select.latest_motion', 'Living Room') -%}
            true
          {% else -%}
            false
          {% endif -%}

      living_room_activity_predicted:
        friendly_name: 'Living Room Activity Predicted'
        entity_id:
          - binary_sensor.moved_from_kitchen_to_hallway
          - binary_sensor.moved_from_bathroom_to_hallway
        value_template: >
          {%- if is_state('input_boolean.living_room_activity_override', 'on') 
          or is_state('input_boolean.living_room_separate_light_profile', 'on')
          or is_state('input_select.living_room_room_state', 'active')  -%}
            false
          {%- elif is_state('binary_sensor.moved_from_kitchen_to_hallway', 'on')
          or is_state('binary_sensor.moved_from_bathroom_to_hallway', 'on')-%}
            true
          {% else -%}
            false
          {% endif -%}

group:
  living_room_activity_entities:
    name: Living Room Activity Entities
    entities:
      - input_boolean.living_room_activity_override
      - input_select.living_room_state
      - input_select.latest_motion
      - binary_sensor.livingroom_sensor
      - binary_sensor.spisebord_bevaegelse
      - binary_sensor.sofagruppe_bevaegelse
      - media_player.stue_chromecast

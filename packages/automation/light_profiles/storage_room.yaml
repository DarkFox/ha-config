script:
  light_profile_storage_room_bright:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: 0
          color_temp: 256
          entity_id:
            - light.klaedeskab
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          entity_id:
            - light.klaedeskab

  light_profile_storage_room_default:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: 0
          color_temp: 330
          entity_id:
            - light.klaedeskab
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 255
          entity_id:
            - light.klaedeskab

  light_profile_storage_room_dimmed:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: 0
          color_temp: 330
          entity_id:
            - light.klaedeskab
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 127
          entity_id:
            - light.klaedeskab

  light_profile_storage_room_ambient:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: 0
          color_temp: 330
          entity_id:
            - light.klaedeskab
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 72
          entity_id:
            - light.klaedeskab

  light_profile_storage_room_night:
    sequence:
      # White temp lights
      - service: light.turn_on
        data_template:
          transition: 0
          color_temp: 500
          entity_id:
            - light.klaedeskab
      - service: light.turn_on
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          brightness: 1
          entity_id:
            - light.klaedeskab

  light_profile_storage_room_off:
    sequence:
      # Primary RGB lights
      - service: light.turn_off
        data_template:
          transition: "{{ (transition_time or 0.5) | float }}"
          entity_id:
            - light.klaedeskab

input_boolean:
  storage_room_activity_override:
    icon: mdi:checkbox-multiple-marked
    name: Storage Room Activity Override
  storage_room_separate_light_profile:
    name: Storage Room Separate Light Profile
    icon: mdi:heart-broken

input_select:
  storage_room_room_state:
    name: Storage Room State
    options:
      - active
      - inactive
      - 'off'

  storage_room_light_profile:
    name: storage room Light Profile
    options:
      - Bright
      - Default
      - Dimmed
      - Ambient
      - Night
      - 'Off'

  storage_room_inactive_light_profile:
    options:
      - Default
      - Dimmed
      - Ambient
      - Night
      - 'Off'

light:
  - platform: template
    lights:
      storage_room_activity_override:
        friendly_name: Storage Room Activity Override
        icon_template: mdi:checkbox-multiple-marked
        value_template: "{{ is_state('input_boolean.storage_room_activity_override', 'on') }}"
        turn_on:
          service: input_boolean.turn_on
          data:
            entity_id: input_boolean.storage_room_activity_override
        turn_off:
          service: input_boolean.turn_off
          data:
            entity_id: input_boolean.storage_room_activity_override

      storage_room_separate_light_profile:
        friendly_name: Storage Room Separate Light Profile
        icon_template: mdi:heart-broken
        value_template: "{{ is_state('input_boolean.storage_room_separate_light_profile', 'on') }}"
        turn_on:
          service: input_boolean.turn_on
          data:
            entity_id: input_boolean.storage_room_separate_light_profile
        turn_off:
          service: input_boolean.turn_off
          data:
            entity_id: input_boolean.storage_room_separate_light_profile

      storage_room_light_profile_bright:
        friendly_name: Lyst
        icon_template: "mdi:brightness-7"
        value_template: "{{ is_state('input_select.storage_room_light_profile', 'Bright') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.storage_room_light_profile
            option: Bright
        turn_off:
          event: storage_room_light_profile_off_null

      storage_room_light_profile_default:
        friendly_name: Normal
        icon_template: "mdi:brightness-5"
        value_template: "{{ is_state('input_select.storage_room_light_profile', 'Default') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.storage_room_light_profile
            option: Default
        turn_off:
          event: storage_room_light_profile_off_null

      storage_room_light_profile_dimmed:
        friendly_name: Dæmpet
        icon_template: "mdi:brightness-6"
        value_template: "{{ is_state('input_select.storage_room_light_profile', 'Dimmed') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.storage_room_light_profile
            option: Dimmed
        turn_off:
          event: storage_room_light_profile_off_null

      storage_room_light_profile_ambient:
        friendly_name: Baggrund
        icon_template: "mdi:brightness-4"
        value_template: "{{ is_state('input_select.storage_room_light_profile', 'Ambient') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.storage_room_light_profile
            option: Ambient
        turn_off:
          event: storage_room_light_profile_off_null

      storage_room_light_profile_night:
        friendly_name: Nat
        icon_template: "mdi:brightness-3"
        value_template: "{{ is_state('input_select.storage_room_light_profile', 'Night') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.storage_room_light_profile
            option: Night
        turn_off:
          event: storage_room_light_profile_off_null

      storage_room_light_profile_off:
        friendly_name: 'Slukket'
        icon_template: "mdi:power"
        value_template: "{{ is_state('input_select.storage_room_light_profile', 'Off') }}"
        turn_on:
          service: input_select.select_option
          data:
            entity_id: input_select.storage_room_light_profile
            option: 'Off'
        turn_off:
          event: storage_room_light_profile_off_null

binary_sensor:
  - platform: template
    sensors:
      storage_room_activity:
        friendly_name: 'Storage Room Activity'
        entity_id:
          - binary_sensor.skab_bevaegelse
          - input_boolean.storage_room_activity_override
          - input_select.storage_room_state
        value_template: >
          {%- if is_state('input_boolean.storage_room_activity_override', 'on') -%}
            {{ 'true' if is_state('input_select.storage_room_room_state', 'active') else 'false' }}
          {%- elif is_state('binary_sensor.skab_bevaegelse', 'on') -%}
            true
          {% else -%}
            false
          {% endif -%}

      storage_room_activity_predicted:
        friendly_name: 'Storage Room Activity Predicted'
        value_template: "false"

group:
  storage_rooom_activity_entities:
    name: Storage Room Activity Entities
    entities:
      - input_boolean.storage_room_activity_override
      - input_select.storage_room_state
      - binary_sensor.skab_bevaegelse

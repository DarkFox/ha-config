input_number:
  tradfri_dimmer:
    name: Trådfri Dimmer
    min: 0
    max: 100
    step: 10

script:
  toggle_activity_override:
    fields:
      room:
        description: "Room to toggle activity override for"
        example: "living_room"
    variables:
      room_name_translation:
        "living_room": "stue"
        "kitchen": "køkken"
        "bedroom": "soveværelse"
        "bathroom": "badeværelse"
        "office": "kontor"
        "workshop": "værksted"
        "hallway": "gang"
    sequence:
      - service: input_boolean.toggle
        data:
          entity_id: "input_boolean.{{ room }}_activity_override"
      - service: script.tts_speak
        data:
          cache: false
          entity_id: "{{ state_attr('group.'+room+'_speakers' , 'entity_id') }}"
          volume_level: "{{ 0.35 if states('input_select.sleep_state') in ['alarm', 'soozing', 'waking'] else 0.60 }}"
          message: "{{ room_name_translation[room] }} overstyring {{'tilkoblet' if states('input_boolean.' ~ room ~ '_activity_override') == 'on' else 'frakoblet'}}"
  toggle_separate_light_profile:
    fields:
      room:
        description: "Room to toggle separate light profile for"
        example: "living_room"
    variables:
      room_name_translation:
        "living_room": "stue"
        "kitchen": "køkken"
        "bedroom": "soveværelse"
        "bathroom": "badeværelse"
        "office": "kontor"
        "workshop": "værksted"
        "hallway": "gang"
    sequence:
      - service: input_boolean.toggle
        data:
          entity_id: "input_boolean.{{ room }}_separate_light_profile"
      - service: script.tts_speak
        data:
          cache: false
          entity_id: "{{ state_attr('group.'+room+'_speakers' , 'entity_id') }}"
          volume_level: "{{ 0.35 if states('input_select.sleep_state') in ['alarm', 'soozing', 'waking'] else 0.60 }}"
          message: "{{ room_name_translation[room] }} separat lysstyring {{'tilkoblet' if states('input_boolean.' ~ room ~ '_separate_light_profile') == 'on' else 'frakoblet'}}"

  remote_switch_on:
    fields:
      room:
        description: "Room to turn lights on or up for"
        example: "living_room"
    sequence:
      - choose:
          - conditions:
              - alias: "Activity override on and room state off"
                condition: template
                value_template: >-
                  {{
                    is_state('input_boolean.' + room + '_activity_override', 'on')
                    and is_state('input_boolean.' + room + '_room_state', 'off')
                  }}
            sequence:
              - service: input_select.select_option
                data:
                  entity_id: "input_select.{{ room }}_room_state"
                  option: "active"
        default:
          - event: cycle_light_profile
            event_data:
              room: "{{ room }}"

  remote_switch_off:
    fields:
      room:
        description: "Room to turn lights off or down for"
        example: "living_room"
    sequence:
      - choose:
          - conditions:
              - alias: "Activity override on"
                condition: template
                value_template: >-
                  {{ is_state('input_boolean.' + room + '_activity_override', 'on') }}
            sequence:
              - service: input_select.select_option
                data:
                  entity_id: "input_select.{{ room }}_room_state"
                  option: "off"
        default:
          - event: decrease_light_profile
            event_data:
              room: "{{ room }}"

automation:
  # Trådfri Dimmer, ID: tradfri_wireless_dimmer #
  - alias: tradfri_dimmer_cw
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 1002
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 2002
    action:
      service: input_number.increment
      data:
        entity_id: input_number.tradfri_dimmer

  - alias: tradfri_dimmer_ccw
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 3002
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 4002
    action:
      service: input_number.decrement
      data:
        entity_id: input_number.tradfri_dimmer

  # gang_fjernbetjening #
  - alias: hallway_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: gang_fjernbetjening
          event: 1002
    action:
      - event: set_light_profile
        event_data:
          room: hallway
          profile: |
            {% if is_state('sensor.hallway_light_profile', 'Off') %}
              Default
            {% else %}
              Off
            {% endif %}

  - alias: hallway_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: gang_fjernbetjening
          event: 2002
    action:
      - event: increase_light_profile
        event_data:
          room: hallway

  - alias: hallway_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: gang_fjernbetjening
          event: 3002
    action:
      - event: decrease_light_profile
        event_data:
          room: hallway

  - alias: hallway_dimmer_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: gang_fjernbetjening
          event: 4002
    action:
      service: switch.toggle
      data:
        entity_id: switch.dorklokke_forstyr_ikke

  - alias: hallway_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: gang_fjernbetjening
          event: 5002
    action:
      service: lock.lock
      target:
        entity_id: lock.hoveddor_las

  - alias: hallway_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: gang_fjernbetjening
          event: 5003
    action:
      service: lock.unlock
      target:
        entity_id: lock.hoveddor_las

  # Kontor Tryk #
  - alias: office_switch_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_tryk
          event: 1002
    action:
      - service: script.remote_switch_on
        data:
          room: office

  - alias: office_switch_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_tryk
          event: 2002
    action:
      - service: script.remote_switch_off
        data:
          room: office

  - alias: office_activity_override_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_tryk
          event: 1003
    action:
      - service: script.toggle_activity_override
        data:
          room: office

  - alias: office_separate_light_profile_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_tryk
          event: 2003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: office

  # Bedroom Remote, ID: sovevaerelse_fjernbetjening #
  - alias: bedroom_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 1002
    action:
      - event: set_light_profile
        event_data:
          room: bedroom
          profile: >
            {% if is_state('sensor.bedroom_light_profile', 'Off') %}
              Default
            {% else %}
              Off
            {% endif %}

  - alias: bedroom_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 2002
    action:
      - event: increase_light_profile
        event_data:
          room: bedroom

  - alias: bedroom_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 3002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: "{{ (states('input_select.sleep_state') not in ['smart wake', 'alarm', 'snoozing', 'waking']) }}"
    action:
      - event: decrease_light_profile
        event_data:
          room: bedroom

  - alias: bedroom_dimmer_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 4002
    action:
      service: cover.open_cover
      data:
        entity_id: cover.sovevaerelse_gardin

  - alias: bedroom_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 5002
    action:
      service: cover.close_cover
      data:
        entity_id: cover.sovevaerelse_gardin

  - alias: bedroom_dimmer_hold_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 5003
    action:
      service: cover.toggle
      data:
        entity_id: cover.sovevaerelse_gardin_vindue

  - alias: bedroom_dimmer_hold_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 4003
    action:
      service: cover.toggle
      data:
        entity_id: cover.sovevaerelse_gardin_dor

  # Office Blind Remote kontor_gardin_fjernbetjening #
  - alias: kontor_gardin_fjernbetjening_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_gardin_fjernbetjening
          event: 1002
    action:
      - service: cover.open_cover
        data:
          entity_id:
            - cover.kontor_rullegardin
            - cover.kontor_altandor_gardin

  - alias: kontor_gardin_fjernbetjening_dn
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_gardin_fjernbetjening
          event: 2002
    action:
      - service: cover.close_cover
        data:
          entity_id:
            - cover.kontor_rullegardin
            - cover.kontor_altandor_gardin

  - alias: kontor_gardin_fjernbetjening_hold
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_gardin_fjernbetjening
          event: 1003
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_gardin_fjernbetjening
          event: 2003
    action:
      - service: cover.set_cover_position
        data:
          entity_id:
            - cover.kontor_rullegardin
            - cover.kontor_altandor_gardin
          position: 50

  # Værksted Remote (Hue Dimmer) vaerksted_fjernbetjening #
  - alias: vaerksted_fjernbetjening_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 1002
    action:
      - choose:
          - conditions:
              - condition: state
                entity_id: input_boolean.workshop_activity_override
                state: "on"
              - condition: state
                entity_id: input_select.workshop_room_state
                state: "off"
            sequence:
              - service: input_select.select_option
                data:
                  entity_id: input_select.workshop_room_state
                  option: "active"
        default:
          - event: set_light_profile
            event_data:
              profile: "Default"
              room: workshop

  - alias: vaerksted_fjernbetjening_on_hold
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 1003
    action:
      - service: script.toggle_activity_override
        data:
          room: workshop

  - alias: vaerksted_fjernbetjening_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 2002
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 2001
    action:
      - event: increase_light_profile
        event_data:
          room: workshop

  - alias: vaerksted_fjernbetjening_dn
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 3002
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 3001
    action:
      - event: decrease_light_profile
        event_data:
          room: workshop

  - alias: vaerksted_fjernbetjening_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 4002
    action:
      - choose:
          - conditions:
              - condition: state
                entity_id: input_boolean.workshop_activity_override
                state: "on"
            sequence:
              - service: input_select.select_option
                data:
                  entity_id: input_select.workshop_room_state
                  option: "off"
        default:
          - event: set_light_profile
            event_data:
              profile: "Off"
              room: workshop

  - alias: vaerksted_fjernbetjening_off_hold
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: vaerksted_fjernbetjening
          event: 4003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: workshop

  # Living room IKEA remote, ID: stue_fjernbetjening #
  - alias: living_room_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 1002
    action:
      - event: set_light_profile
        event_data:
          room: living_room
          profile: >
            {% if is_state('sensor.living_room_light_profile', 'Off') %}
              Default
            {% else %}
              Off
            {% endif %}

  - alias: living_room_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 2002
    action:
      - event: increase_light_profile
        event_data:
          room: living_room

  - alias: living_room_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 3002
    action:
      - event: decrease_light_profile
        event_data:
          room: living_room

  - alias: living_room_dimmer_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 4002
    action:
      service: cover.open_cover
      data:
        entity_id: cover.stue_gardin

  - alias: living_room_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 5002
    action:
      service: cover.close_cover
      data:
        entity_id: cover.stue_gardin

  - alias: living_room_dimmer_hold_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 5003
    action:
      service: cover.toggle
      data:
        entity_id: cover.stue_gardin_hojre

  - alias: living_room_dimmer_hold_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 4003
    action:
      service: cover.toggle
      data:
        entity_id: cover.stue_gardin_venstre

  # Stue Tryk #
  - alias: living_room_switch_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_tryk
          event: 1002
    action:
      - service: script.remote_switch_on
        data:
          room: living_room

  - alias: living_room_switch_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_tryk
          event: 2002
    action:
      - service: script.remote_switch_off
        data:
          room: living_room

  - alias: living_room_activity_override_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_tryk
          event: 1003
    action:
      - service: script.toggle_activity_override
        data:
          room: living_room

  - alias: living_room_separate_light_profile_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_tryk
          event: 2003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: living_room

  # Kitchen IKEA remote, ID: kokken_fjernbetjening #
  - alias: kokken_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 1002
    action:
      - event: set_light_profile
        event_data:
          room: kitchen
          profile: >
            {% if is_state('sensor.kitchen_light_profile', 'Off') %}
              Default
            {% else %}
              Off
            {% endif %}

  - alias: kokken_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 2002
    action:
      - event: increase_light_profile
        event_data:
          room: kitchen

  - alias: kokken_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 3002
    action:
      - event: decrease_light_profile
        event_data:
          room: kitchen

  - alias: kokken_dimmer_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 4002
    action:
      service: light.toggle
      data:
        entity_id: light.kitchen_sink

  - alias: kokken_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 5002
    action:
      service: light.toggle
      data:
        entity_id: light.kitchen_stove

  - alias: kokken_dimmer_hold_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 2003
    action:
      - service: script.toggle_activity_override
        data:
          room: kitchen

  - alias: kokken_dimmer_hold_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 3003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: kitchen

  - alias: kokken_dimmer_hold_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 4003
    action:
      - delay:
          seconds: 0

  - alias: kokken_dimmer_hold_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 5003
    action:
      service: switch.toggle
      data:
        entity_id: switch.elkedel

  # Køkken Tryk #
  - alias: kitchen_switch_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_tryk
          event: 1002
    action:
      - service: script.remote_switch_on
        data:
          room: kitchen

  - alias: kitchen_switch_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_tryk
          event: 2002
    action:
      - service: script.remote_switch_off
        data:
          room: kitchen

  - alias: kitchen_activity_override_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_tryk
          event: 1003
    action:
      - service: script.toggle_activity_override
        data:
          room: kitchen

  - alias: kitchen_separate_light_profile_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_tryk
          event: 2003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: kitchen

  # Badeværelse Tryk #
  - alias: bathroom_switch_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_tryk
          event: 1002
    action:
      - service: script.remote_switch_on
        data:
          room: bathroom

  - alias: bathroom_switch_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_tryk
          event: 2002
    action:
      - service: script.remote_switch_off
        data:
          room: bathroom

  - alias: bathroom_activity_override_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_tryk
          event: 1003
    action:
      - service: script.toggle_activity_override
        data:
          room: bathroom

  - alias: bathroom_separate_light_profile_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_tryk
          event: 2003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: bathroom

  # Badevaerelse yderside tryk #

  - alias: bathroom_outside_switch_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_yderside_tryk
          event: 1002
    condition:
      - alias: "Guest Mode Off"
        condition: state
        entity_id: input_boolean.guests
        state: "off"
    action:
      - service: script.remote_switch_on
        data:
          room: bathroom

  - alias: bathroom_outside_switch_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_yderside_tryk
          event: 2002
    condition:
      - alias: "Guest Mode Off"
        condition: state
        entity_id: input_boolean.guests
        state: "off"
    action:
      - service: script.remote_switch_off
        data:
          room: bathroom

  - alias: bathroom_outside_switch_on_guest_mode
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_yderside_tryk
          event: 1002
    condition:
      - alias: "Guest Mode On"
        condition: state
        entity_id: input_boolean.guests
        state: "on"
    action:
      - alias: "Flip bathroom state to on"
        service: input_select.select_option
        target:
          entity_id: input_select.bathroom_room_state
        data:
          option: "active"

  - alias: bathroom_outside_switch_off_guest_mode
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: badevaerelse_yderside_tryk
          event: 2002
    condition:
      - alias: "Guest Mode On"
        condition: state
        entity_id: input_boolean.guests
        state: "on"
    action:
      - alias: "Flip bathroom state to off"
        service: input_select.select_option
        target:
          entity_id: input_select.bathroom_room_state
        data:
          option: "off"

  # Altan Tryk #
  - alias: balcony_switch_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: altan_tryk
          event: 1002
    action:
      - service: script.remote_switch_on
        data:
          room: balcony

  - alias: balcony_switch_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: altan_tryk
          event: 2002
    action:
      - service: script.remote_switch_off
        data:
          room: balcony

  - alias: balcony_activity_override_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: altan_tryk
          event: 1003
    action:
      - service: script.toggle_activity_override
        data:
          room: balcony

  - alias: balcony_separate_light_profile_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: altan_tryk
          event: 2003
    action:
      - service: script.toggle_separate_light_profile
        data:
          room: balcony

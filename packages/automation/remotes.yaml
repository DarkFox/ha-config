input_number:
  tradfri_dimmer:
    name: Trådfri Dimmer
    min: 0
    max: 100
    step: 10

automation:
  # Trådfri Dimmer, ID: tradfri_wireless_dimmer #
  - alias: tradfri_dimmer_cw
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 1002
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 2002
    action:
      service: input_number.increment
      data_template:
        entity_id: input_number.tradfri_dimmer

  - alias: tradfri_dimmer_ccw
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 3002
      - platform: event
        event_type: deconz_event
        event_data:
          id: tradfri_wireless_dimmer
          event: 4002
    action:
      service: input_number.decrement
      data_template:
        entity_id: input_number.tradfri_dimmer

   # Office Hue Dimmer, ID: kontor_fjernbetjening #
  - alias: office_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 1002
    action:
      service: input_select.select_option
      data:
        option: 'Default'
        entity_id: input_select.office_light_profile

  - alias: office_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 2002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {{ states.input_select.office_light_profile.state != 'Bright' }}
    action:
      service: input_select.select_previous
      data:
        entity_id: input_select.office_light_profile

  - alias: office_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 3002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {{ states.input_select.office_light_profile.state != 'Off' }}
    action:
      service: input_select.select_next
      data:
        entity_id: input_select.office_light_profile

  - alias: office_dimmer_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 4002
    action:
      service: input_select.select_option
      data:
        option: 'Off'
        entity_id: input_select.office_light_profile

  - alias: office_activity_override_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 1003
    action:
      - service: input_boolean.toggle
        data:
          entity_id: input_boolean.office_activity_override
      - service: tts.google_cloud_say
        data_template:
          entity_id: media_player.kontor_mini
          message: "Kontor overstyring {{'tilkoblet' if states('input_boolean.office_activity_override') == 'on' else 'frakoblet'}}"

  - alias: office_override_room_state_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 2003
    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_boolean.office_activity_override
          state: 'on'
        - condition: template
          value_template: >
            {{ states.input_select.office_room_state.state != 'active' }}
    action:
      - service: input_select.select_previous
        data:
          entity_id: input_select.office_room_state

  - alias: office_override_room_state_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 3003
    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_boolean.office_activity_override
          state: 'on'
        - condition: template
          value_template: >
            {{ states.input_select.office_room_state.state != 'off' }}
    action:
      - service: input_select.select_next
        data:
          entity_id: input_select.office_room_state

  - alias: office_separate_light_profile_toggle
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kontor_fjernbetjening
          event: 4003
    action:
      - service: input_boolean.toggle
        data:
          entity_id: input_boolean.office_separate_light_profile
      - service: tts.google_cloud_say
        data_template:
          entity_id: media_player.kontor_mini
          message: "Kontor separat lysstyring {{'tilkoblet' if states('input_boolean.office_separate_light_profile') == 'on' else 'frakoblet'}}"

  # Bedroom Hue Dimmer, ID: sovevaerelse_fjernbetjening #
  - alias: bedroom_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 1002
    action:
      service: input_select.select_option
      data_template:
        option: 'Default'
        entity_id: >
          {% if is_state('input_boolean.guests', 'on') %}
            input_select.bedroom_light_profile
          {% else %}
            input_select.light_profile
          {% endif %}

  - alias: bedroom_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 2002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {% if is_state('input_boolean.guests', 'on') %}
              {{ states.input_select.bedroom_light_profile.state != 'Bright' }}
            {% else %}
              {{ states.input_select.light_profile.state != 'Bright' }}
            {% endif %}
    action:
      service: input_select.select_previous
      data_template:
        entity_id: >
          {% if is_state('input_boolean.guests', 'on') %}
            input_select.bedroom_light_profile
          {% else %}
            input_select.light_profile
          {% endif %}

  - alias: bedroom_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 3002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: "{{ (states('input_select.sleep_state') not in ['smart wake', 'alarm', 'snoozing', 'waking']) }}"
        - condition: template
          value_template: >
            {% if is_state('input_boolean.guests', 'on') %}
              {{ states.input_select.bedroom_light_profile.state != 'Off' }}
            {% else %}
              {{ states.input_select.light_profile.state != 'Off' }}
            {% endif %}
    action:
      service: input_select.select_next
      data_template:
        entity_id: >
          {% if is_state('input_boolean.guests', 'on') %}
            input_select.bedroom_light_profile
          {% else %}
            input_select.light_profile
          {% endif %}

  - alias: bedroom_dimmer_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 4002
    condition:
      condition: template
      value_template: "{{ (states('input_select.sleep_state') not in ['smart wake', 'alarm', 'snoozing', 'waking']) }}"
    action:
      service: input_select.select_option
      data_template:
        option: 'Off'
        entity_id: >
          {% if is_state('input_boolean.guests', 'on') %}
            input_select.bedroom_light_profile
          {% else %}
            input_select.light_profile
          {% endif %}

  - alias: bedroom_dimmer_bedside_bright
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 1003
    action:
      service: light.turn_on
      data:
        brightness: 255
        color_temp: 350
        entity_id:
          - light.sengelampe

  - alias: bedroom_dimmer_bedside_dimmed
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 2003
    action:
      service: light.turn_on
      data:
        brightness: 32
        color_temp: 500
        entity_id:
          - light.sengelampe

  - alias: bedroom_dimmer_bedside_night
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 3003
    action:
      service: light.turn_on
      data:
        brightness: 2
        rgb_color: [255,0,0]
        entity_id:
          - light.sengelampe

  - alias: bedroom_dimmer_bedside_off
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: sovevaerelse_fjernbetjening
          event: 4003
    action:
      service: light.turn_off
      data:
        entity_id:
          - light.sengelampe

  # Living room IKEA remote, ID: stue_fjernbetjening #
  - alias: living_room_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 1002
    action:
      service: input_select.select_option
      data_template:
        option: >
          {% if is_state('input_select.living_room_light_profile', 'Off') %}
            Default
          {% else %}
            Off
          {% endif %}
        entity_id: input_select.living_room_light_profile

  - alias: living_room_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 2002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {{ states.input_select.living_room_light_profile.state != 'Bright' }}
    action:
      service: input_select.select_previous
      data:
        entity_id: input_select.living_room_light_profile

  - alias: living_room_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 3002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {{ states.input_select.living_room_light_profile.state != 'Off' }}
    action:
      service: input_select.select_next
      data:
        entity_id: input_select.living_room_light_profile

  - alias: living_room_dimmer_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 4002
    action:
      service: input_select.select_previous
      data:
        entity_id: input_select.theme

  - alias: living_room_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: stue_fjernbetjening
          event: 5002
    action:
      service: input_select.select_next
      data:
        entity_id: input_select.theme

  # Kitchen IKEA remote, ID: kokken_fjernbetjening #
  - alias: kokken_dimmer_on
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 1002
    action:
      service: input_select.select_option
      data_template:
        option: >
          {% if is_state('input_select.kitchen_light_profile', 'Off') %}
            Default
          {% else %}
            Off
          {% endif %}
        entity_id: input_select.kitchen_light_profile

  - alias: kokken_dimmer_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 2002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {{ states.input_select.kitchen_light_profile.state != 'Bright' }}
    action:
      service: input_select.select_previous
      data:
        entity_id: input_select.kitchen_light_profile

  - alias: kokken_dimmer_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 3002
    condition:
      condition: and
      conditions:
        - condition: template
          value_template: >
            {{ states.input_select.kitchen_light_profile.state != 'Off' }}
    action:
      service: input_select.select_next
      data:
        entity_id: input_select.kitchen_light_profile

  - alias: kokken_dimmer_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 4002
    action:
      service: light.toggle
      data:
        entity_id: light.kitchen_sink

  - alias: kokken_dimmer_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 5002
    action:
      service: light.toggle
      data:
        entity_id: light.kitchen_stove

  - alias: kokken_dimmer_hold_up
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 2003
    action:
      - service: input_boolean.toggle
        data:
          entity_id: input_boolean.kitchen_activity_override
      - service: tts.google_cloud_say
        data_template:
          entity_id: media_player.kokken_mini
          message: "Køkken overstyring {{'tilkoblet' if states('input_boolean.kitchen_activity_override') == 'on' else 'frakoblet'}}"

  - alias: kokken_dimmer_hold_down
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 3003
    action:
      - service: input_boolean.toggle
        data:
          entity_id: input_boolean.kitchen_separate_light_profile
      - service: tts.google_cloud_say
        data_template:
          entity_id: media_player.kokken_mini
          message: "Køkken separat lysstyring {{'tilkoblet' if states('input_boolean.kitchen_separate_light_profile') == 'on' else 'frakoblet'}}"

  - alias: kokken_dimmer_hold_left
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 4003
    action:
      service: switch.toggle
      data:
        entity_id: switch.kokken_tv_afbryder

  - alias: kokken_dimmer_hold_right
    initial_state: true
    trigger:
      - platform: event
        event_type: deconz_event
        event_data:
          id: kokken_fjernbetjening
          event: 5003
    action:
      service: switch.toggle
      data:
        entity_id: switch.elkedel


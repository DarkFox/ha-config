automation:
  - alias: activate_light_profile_workshop
    id: b95904b0-7a60-4c6e-bdaf-1c6fe7a98df2
    mode: restart
    initial_state: true
    triggers:
      - trigger: state
        entity_id: sensor.workshop_active_light_profile
      - trigger: event
        event_type: refresh_light_profile
    variables:
      profile: "{{ states('sensor.workshop_active_light_profile') }}"
    actions:
      - choose:
          - conditions:
              - condition: template
                value_template: "{{ profile == 'Bright' }}"
            sequence:
              - action: scene.apply
                data:
                  transition: "{{ (states('input_number.scene_transition_time') | float or 0.5) }}"
                  entities:
                    light.vaerksted_loftlampe:
                      state: on
                      brightness: 255
                      color_temp_kelvin: 4000
                    light.hobbybord_bord:
                      state: on
                      brightness: 255
                      effect: "None"
                    light.hobbybord_vaerktojsskab:
                      state: on
                      brightness: 255
                      effect: "None"

          - conditions:
              - condition: template
                value_template: "{{ profile == 'Default' }}"
            sequence:
              - action: scene.apply
                data:
                  transition: "{{ (states('input_number.scene_transition_time') | float or 0.5) }}"
                  entities:
                    light.vaerksted_loftlampe:
                      state: on
                      brightness: 255
                      color_temp_kelvin: 4000
                    light.hobbybord_bord:
                      state: on
                      brightness: 255
                      effect: "None"
                    light.hobbybord_vaerktojsskab:
                      state: on
                      brightness: 255
                      effect: "None"

          - conditions:
              - condition: template
                value_template: "{{ profile == 'Dimmed' }}"
            sequence:
              - action: scene.apply
                data:
                  transition: "{{ (states('input_number.scene_transition_time') | float or 0.5) }}"
                  entities:
                    light.vaerksted_loftlampe:
                      state: on

                      brightness: 72
                      hs_color: "{{ state_attr('sensor.theme', 'primary_color_hue') }}"
                    light.hobbybord_bord:
                      state: on
                      brightness: 48
                      effect: "None"
                    light.hobbybord_vaerktojsskab:
                      state: on
                      brightness: 48
                      effect: "None"

          - conditions:
              - condition: template
                value_template: "{{ profile == 'Ambient' }}"
            sequence:
              - action: scene.apply
                data:
                  transition: "{{ (states('input_number.scene_transition_time') | float or 0.5) }}"
                  entities:
                    light.vaerksted_loftlampe:
                      state: on

                      brightness: 48
                      hs_color: "{{ state_attr('sensor.theme', 'primary_color_hue') }}"
                    light.hobbybord_bord:
                      state: on
                      brightness: 36
                      effect: "None"
                    light.hobbybord_vaerktojsskab:
                      state: on
                      brightness: 36
                      effect: "None"

          - conditions:
              - condition: template
                value_template: "{{ profile == 'Night' }}"
            sequence:
              - action: scene.apply
                data:
                  transition: "{{ (states('input_number.scene_transition_time') | float or 0.5) }}"
                  entities:
                    light.vaerksted_loftlampe:
                      state: on

                      brightness: 2
                      rgb_color: [255, 0, 0]
                    light.hobbybord_bord:
                      state: on
                      brightness: 8
                      effect: "None"
                    light.hobbybord_vaerktojsskab:
                      state: off

          - conditions:
              - condition: template
                value_template: "{{ profile == 'Off' }}"
            sequence:
              - action: scene.apply
                data:
                  transition: "{{ (states('input_number.scene_transition_time') | float or 0.5) }}"
                  entities:
                    light.vaerksted_lys:
                      state: off

input_boolean:
  workshop_separate_light_profile:
    name: Workshop Separate Light Profile
    icon: mdi:heart-broken

input_select:
  workshop_light_profile:
    name: Workshop Light Profile
    options:
      - Bright
      - Default
      - Dimmed
      - Ambient
      - Night
      - "Off"

template:
  - sensor:
      - name: "Workshop Selected Light Profile"
        unique_id: dde1f6a7-c01c-4c28-848c-92376ec562c2
        state: >-
          {%- from 'light_profile.jinja' import room_selected_light_profile -%}
          {{ room_selected_light_profile('workshop') }}

      - name: "Workshop Auto Light Profile"
        unique_id: c4b0d322-4283-46c7-9d82-5fd8ad8ede9d
        state: >-
          {%- from 'light_profile.jinja' import room_auto_light_profile -%}
          {{ room_auto_light_profile('workshop') }}

      - name: "Workshop Light Profile"
        unique_id: 509c455d-9d5b-4c04-9911-abbd6d61d4e8
        state: >-
          {%- from 'light_profile.jinja' import room_light_profile -%}
          {{ room_light_profile('workshop') }}

      - name: "Workshop Inactive Light Profile"
        unique_id: a01b2906-dfac-4d9d-8556-a1b7966a2eb9
        state: >-
          {%- from 'light_profile.jinja' import room_inactive_light_profile -%}
          {{ room_inactive_light_profile('workshop') }}

      - name: "Workshop Active Light Profile"
        unique_id: c9bfc0e5-d0e6-4206-8cff-2d79c4a06809
        state: >-
          {%- from 'light_profile.jinja' import room_active_light_profile -%}
          {{ room_active_light_profile('workshop') }}

  - light:
      - default_entity_id: light.workshop_separate_light_profile
        unique_id: 019af532-1132-7742-a302-9bba3c08bb6f
        name: Light Workshop Separate Light Profile
        icon: mdi:heart-broken
        state: "{{ is_state('input_boolean.workshop_separate_light_profile', 'on') }}"
        turn_on:
          action: input_boolean.turn_on
          data:
            entity_id: input_boolean.workshop_separate_light_profile
        turn_off:
          action: input_boolean.turn_off
          data:
            entity_id: input_boolean.workshop_separate_light_profile

      - default_entity_id: light.workshop_light_profile_bright
        unique_id: 019af532-1143-77fa-81c5-95220ba9b1d7
        name: Lyst
        icon: "mdi:brightness-7"
        state: "{{ is_state('sensor.workshop_selected_light_profile', 'Bright') }}"
        turn_on:
          event: set_light_profile
          event_data:
            room: workshop
            profile: Bright
        turn_off:
          event: workshop_light_profile_off_null

      - default_entity_id: light.workshop_light_profile_default
        unique_id: 019af532-1176-74ea-ae66-945f2e5efd4a
        name: Normal
        icon: "mdi:brightness-5"
        state: "{{ is_state('sensor.workshop_selected_light_profile', 'Default') }}"
        turn_on:
          event: set_light_profile
          event_data:
            room: workshop
            profile: Default
        turn_off:
          event: workshop_light_profile_off_null

      - default_entity_id: light.workshop_light_profile_dimmed
        unique_id: 019af532-11a0-75ff-a599-1995d1d05934
        name: Dæmpet
        icon: "mdi:brightness-6"
        state: "{{ is_state('sensor.workshop_selected_light_profile', 'Dimmed') }}"
        turn_on:
          event: set_light_profile
          event_data:
            room: workshop
            profile: Dimmed
        turn_off:
          event: workshop_light_profile_off_null

      - default_entity_id: light.workshop_light_profile_ambient
        unique_id: 019af532-11ab-7771-b3bb-31c48fa0a88e
        name: Baggrund
        icon: "mdi:brightness-4"
        state: "{{ is_state('sensor.workshop_selected_light_profile', 'Ambient') }}"
        turn_on:
          event: set_light_profile
          event_data:
            room: workshop
            profile: Ambient
        turn_off:
          event: workshop_light_profile_off_null

      - default_entity_id: light.workshop_light_profile_night
        unique_id: 019af532-11bc-754f-b0c6-4bafe17bb432
        name: Nat
        icon: "mdi:brightness-3"
        state: "{{ is_state('sensor.workshop_selected_light_profile', 'Night') }}"
        turn_on:
          event: set_light_profile
          event_data:
            room: workshop
            profile: Night
        turn_off:
          event: workshop_light_profile_off_null

      - default_entity_id: light.workshop_light_profile_off
        unique_id: 019af532-11cd-738f-bd2a-5ca87fb1322a
        name: "Slukket"
        icon: "mdi:power"
        state: "{{ is_state('sensor.workshop_selected_light_profile', 'Off') }}"
        turn_on:
          event: set_light_profile
          event_data:
            room: workshop
            profile: "Off"
        turn_off:
          event: workshop_light_profile_off_null

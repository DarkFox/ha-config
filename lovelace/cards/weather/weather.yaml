type: custom:vertical-stack-in-card
cards:
  - !include simple_weather.yaml

  - !include dmi_weather_warnings.yaml

  - type: custom:mod-card
    style: |
      #htmlCard {
        font-size: 120%;
        background: inherit;
        border: 0px;
      }
    card: !include dmi_weather_report.yaml

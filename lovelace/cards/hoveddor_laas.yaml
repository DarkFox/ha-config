type: custom:button-card
entity: lock.hoveddor_las
name: Hoveddør
tap_action:
  action: call-service
  service: lock.lock
  service_data:
    entity_id: lock.hoveddor_las
hold_action:
  action: call-service
  service: lock.unlock
  service_data:
    entity_id: lock.hoveddor_las
